<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Settings | Memofy</title>
    <link rel="stylesheet" href="/css/admin-dash.css">
    <link rel="stylesheet" href="/css/admintopbar.css">
    <link rel="stylesheet" href="/css/settings.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <!-- Google Analytics Tracking -->
    <%- include('google-analytics', { propertyId: typeof gaPropertyId !== 'undefined' ? gaPropertyId : null }); %>

    <script src="/js/lucide.min.js"></script>
</head>
<body>
    <div class="dashboard-container">
        <%- include('../../components/nav'); %>

        <main class="main-content">
            <%- include('../../components/admintopbar'); %>

            <section class="settings-section">
                <div class="settings-header">
                    <h1>Workspace Settings</h1>
                    <p>Manage your account security and workspace preferences.</p>
                </div>

                <div class="settings-grid">
                    <div class="settings-card">
                        <h3>Change Password</h3>
                        <p>Update your password regularly to keep your account secure.</p>
                        <form id="passwordForm" class="settings-form">
                            <div>
                                <label for="currentPassword">Current Password</label>
                                <input type="password" id="currentPassword" name="currentPassword" autocomplete="current-password" required>
                            </div>
                            <div>
                                <label for="newPassword">New Password</label>
                                <input type="password" id="newPassword" name="newPassword" autocomplete="new-password" required>
                            </div>
                            <div>
                                <label for="confirmPassword">Confirm New Password</label>
                                <input type="password" id="confirmPassword" name="confirmPassword" autocomplete="new-password" required>
                            </div>
                            <div class="settings-actions">
                                <button type="submit" class="settings-btn primary">Update Password</button>
                            </div>
                            <div id="passwordFeedback" class="settings-feedback" role="status"></div>
                        </form>
                    </div>

                    <div class="settings-card">
                        <h3>Security & Appearance</h3>
                        <p>Control how Memofy looks and how you sign in.</p>
                        <div class="preferences-grid">
                            <div class="setting-row">
                                <div class="setting-info">
                                    <h4>Dark Mode</h4>
                                    <p>Switch between light and dark themes.</p>
                                </div>
                                <label class="switch">
                                    <input type="checkbox" id="darkModeToggle" data-setting="darkMode">
                                    <span class="slider"></span>
                                </label>
                            </div>
                            <div class="setting-row">
                                <div class="setting-info">
                                    <h4>Two-Factor Authentication</h4>
                                    <p>Require an OTP sent to your email when signing in.</p>
                                </div>
                                <label class="switch">
                                    <input type="checkbox" id="twoFactorToggle" data-setting="twoFactorEnabled">
                                    <span class="slider"></span>
                                </label>
                            </div>
                        </div>
                    </div>

                    <div class="settings-card">
                        <h3>Session Timeout</h3>
                        <p>Choose how long to keep sessions active while idle.</p>
                        <div class="settings-form">
                            <div>
                                <label for="sessionTimeoutSelect">Auto logout after</label>
                                <select id="sessionTimeoutSelect">
                                    <option value="1">1 minute</option>
                                    <option value="5">5 minutes</option>
                                    <option value="10">10 minutes</option>
                                    <option value="15">15 minutes</option>
                                    <option value="30">30 minutes</option>
                                    <option value="60">1 hour</option>
                                    <option value="1440">24 hours</option>
                                </select>
                            </div>
                            <div class="settings-feedback" id="sessionFeedback" role="status"></div>
                        </div>
                    </div>

                    <div class="settings-card">
                        <h3>Notifications</h3>
                        <p>Stay informed about activity that matters.</p>
                        <div class="preferences-grid">
                            <div class="setting-row">
                                <div class="setting-info">
                                    <h4>New Memo Emails</h4>
                                    <p>Email me whenever new memos are created.</p>
                                </div>
                                <label class="switch">
                                    <input type="checkbox" data-setting="notifications.memoEmails" id="memoEmailToggle">
                                    <span class="slider"></span>
                                </label>
                            </div>
                            <div class="setting-row">
                                <div class="setting-info">
                                    <h4>Profile Updates</h4>
                                    <p>Notify me when users update their details.</p>
                                </div>
                                <label class="switch">
                                    <input type="checkbox" data-setting="notifications.profileUpdates" id="profileUpdateToggle">
                                    <span class="slider"></span>
                                </label>
                            </div>
                        </div>
                        <div class="settings-feedback" id="notificationFeedback" role="status"></div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <script>
        // Initialize Lucide icons
        lucide.createIcons();
    </script>
    <script src="/js/settings.js"></script>
</body>
</html>
